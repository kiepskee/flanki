{
  "name": "flanki",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node --check src/worker.js && node --check public/app.js",
    "dev": "wrangler dev --local",
    "dev:preview": "wrangler dev --env preview --local",
    "deploy": "node -e \"throw new Error('Use npm run deploy:preview or npm run deploy:production explicitly.')\"",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production",
    "db:apply:preview": "wrangler d1 execute flanki-db-preview --env preview --remote --file=./schema.sql",
    "db:apply:production": "wrangler d1 execute flanki-db --env production --remote --file=./schema.sql",
    "db:reset:local": "wrangler d1 execute flanki-db-local --local --file=./schema-reset.sql && wrangler d1 execute flanki-db-local --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
