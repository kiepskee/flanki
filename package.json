{
  "name": "flanki",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node --check src/worker.js && node --check public/app.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute flanki-db --file=./schema.sql",
    "db:reset:local": "wrangler d1 execute flanki-db --local --file=./schema-reset.sql && wrangler d1 execute flanki-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
